<table class="table table-hover">
  <tr>
    <th><%= title %></th>
    <th>Average</th>
  </tr>
  <% top_object.each do |object| %>
    <tr>
      <td><%= object.name %></td>
      <td><%= round(object.average_rating) %></td>
    </tr>
  <% end %>
</table>